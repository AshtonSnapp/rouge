use self::iter::[
	Iter
	IterMut
	Yield
]

@builtin
pub type [T]

impl Iter<T> for [T] is
	@builtin
	func iter(self) -< Yield<T>
end

impl IterMut for [T] is
	@builtin
	func iter_mut(mut self) -< Yield<mut T>
end

@builtin
pub type [K: V]

impl Iter<(K, V)> for [K: V] is
	@builtin
	func iter(self) -< Yield<(K, V)>
end

impl IterMut<(K, mut V)> for [K: V] is
	@builtin
	func iter_mut(mut self) -< Yield<(K, mut V)>
end